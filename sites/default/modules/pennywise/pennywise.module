<?php

/**
 * @file
 *  General hacks.
 *  - Not in use at this time.
 *  - MOre of a placeholder.
 */

define ('DEBUG', TRUE);
 
 
function pennywise_node_view($node, $view_mode, $langcode){
  if(DEBUG) {
    global $user;
    dpm('NODE');
    dpm($node);
    dpm('View Mode');
    dpm($view_mode);
  }
}